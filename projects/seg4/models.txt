# models.txt  (project: seg4)
# Format (pipe-separated):   <models_subdir> | <url> | [optional_filename]
# seg4 = Flux.2 Klein 4B image edit/restyle (light) for the face pipeline.
# Sequential load (encode text -> free -> diffuse) => fits 11GB. Add --lowvram
# if it spikes. For even lower VRAM swap to the GGUF variants (ComfyUI-GGUF).
# NOTE: verify exact filenames in the Comfy-Org/flux2-klein-4B tree — repo
#       paths shift. fp8 weights below.

# Comfy-Org packaged Klein 4B under this (oddly named) repo. Distilled
# 'flux-2-klein-4b' = fast/fewer steps (use this); 'base-4b' needs CFG.
# No separate fp8 file — this IS the packaged weight.

# ---- Flux.2 Klein 4B  (diffusion, distilled) -------------------------------
diffusion_models | https://huggingface.co/Comfy-Org/vae-text-encorder-for-flux-klein-4b/resolve/main/split_files/diffusion_models/flux-2-klein-4b.safetensors

# ---- Text encoder (Qwen3-4B) — 4B Klein pairs with the 4B encoder ----------
# (qwen_3_4b_fp4_flux2 exists too if VRAM tight)
text_encoders | https://huggingface.co/Comfy-Org/vae-text-encorder-for-flux-klein-4b/resolve/main/split_files/text_encoders/qwen_3_4b.safetensors

# ---- VAE (flux2 vae, small) ------------------------------------------------
vae | https://huggingface.co/Comfy-Org/vae-text-encorder-for-flux-klein-4b/resolve/main/split_files/vae/flux2-vae.safetensors

# ---- SAM3 (text-prompt segmentation — head/face masks) ---------------------
# facebook/sam3 is gated -> 1038lab mirror. EasySAM3 may expect a different
# subdir/filename; verify where the node loads it (models/sam3 vs its own dir).
sam3 | https://huggingface.co/1038lab/sam3/resolve/main/sam3.pt

# ---- CodeFormer face restore (facerestore_cf node) -------------------------
facerestore_models | https://github.com/sczhou/CodeFormer/releases/download/v0.1.0/codeformer.pth

# ---- facexlib helper weights (face detect + parse; else auto-pulled at run) -
facexlib | https://github.com/xinntao/facexlib/releases/download/v0.1.0/detection_Resnet50_Final.pth
facexlib | https://github.com/xinntao/facexlib/releases/download/v0.1.0/parsing_parsenet.pth
